## Install roles here using below command line

```bash
# Example role
ansible-galaxy install vikiscripts.bind9
```

```bash
# You can install multiple roles written in a file
ansible-galaxy install -p ../roles -r vagrant-roles.yml
```
